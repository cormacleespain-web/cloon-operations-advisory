# Changelog

- 2026-07-09 — project bootstrap (Next.js 16 + Tailwind + shadcn + Neon + Auth.js), linked to Vercel + GitHub.
- 2026-07-09 — cloon-landing — shipped, inline QA clean, production build passing — single-page scroll marketing site (hero, services, approach, about, contact + Resend form, light/dark) — authority: brand + ui-ux-pro-max.
- 2026-07-09 — cloon-premium-restyle — shipped, QA clean, build passing — premium elevation: fixed hero/header logo duplication; editorial serif hero + animated facet centerpiece; navy Approach/Positioning/Footer bands; double-bezel cards; floating glass pill nav; button-in-button CTAs; grain overlay; blur-fade staggered reveals. impeccable polish removed numbered-eyebrow AI-tells — authority: high-end-visual-design + impeccable.
- 2026-07-09 — contact form wired to live Resend sending (env-only, no code changes). Provisioned Resend via Vercel integration; `CONTACT_TO_EMAIL` set to cormacleespain@gmail.com (Resend sandbox mode only delivers to the account's own address) as a temporary demo inbox — frontend still displays hello@cloon.ie throughout. Verified end-to-end via Preview MCP.
- 2026-07-20 — admin CMS (`feature/admin-cms`, 11 commits) merged to `main` and pushed — `content_sections` key→JSONB in Neon, Zod schemas + typed defaults, per-section editors, publish/draft/revisions, Vercel Blob media library, Auth.js credentials login, draft preview. Zero visible change on the deployed site (content identical); admin is the new baseline for everything below.
- 2026-07-20 — client-copy-pages — local branch `feature/client-copy-pages` (never pushed/merged, Cormac reviews and ships himself). Multi-page restructure around Conor's first real copy batch (4 Word docs, 17 Jul 2026): new subpages `/business-challenges`, `/how-i-work`, `/my-story` with verbatim-locked client wording (curly quotes, missing periods, the "ERP system..." ellipsis all preserved); home restructured to hero → homeIntro → homeTeasers → homeExperience → contact as a high-level lead-in, old services/approach/about/positioning sections retired; header/footer nav converted to `next/link` with route-aware active state; new `ScrollInk` component adds a CSS-only scroll-linked ink-in effect (`animation-timeline: view()`, zero JS, full static fallback) on both pull quotes, the home intro, How I Work's experience paragraph, and each review's "what's really happening" paragraph. Copy verified against source via an automated flatten-and-diff script (caught one dropped subheading, fixed). DB cutover for the stale pre-restructure `hero` row is a documented go-live step, not run this session — see `oneshot/qa/client-copy-pages.md`. Authority: high-end-visual-design + impeccable (unchanged).
